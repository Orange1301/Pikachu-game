#include <iostream>
#include <string>
#include "Controller.h"
#include "Game.h"
#include <random>
#include <fstream>

struct Menu {
	static int currentOption;
	const static string options[5];
	static 
}

struct Player {
	string playerName;
	int score;
	string mode;
};